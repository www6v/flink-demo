<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flink.common.mybatis.mapper.ExceptionMapper">

    <insert id="insertException" parameterType="ExceptionPO">
       insert into user_exception(app_id, room_id,	user_id, status_type, time, exception_type)
        values (#{appId}, #{roomId}, #{userId}, #{statusType}, #{time}, #{exceptionType})
    </insert>

</mapper>


